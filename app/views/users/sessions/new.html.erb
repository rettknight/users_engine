<%= simple_form_for(:session, :url => sessions_path) do |f| %>
    <%= f.input :email, input_html: {style: 'width: 200px;'} %>
    <%= f.input :password, as: :password, input_html: {style: 'width: 200px;'}  %>
    <div class = "field">
      <%= link_to 'Contraseña invalida?', new_password_reset_path %>
    </div>
    <div class="field">
      <%= check_box_tag :remember_me, 1, params[:remember_me] %>
      <%= label_tag :remember_me, 'Recordar sesion?' %>
    </div>
    <%= f.button :submit, value: 'Iniciar', class: 'btn btn-primary' %>
    <div class="field">
        <%= link_to 'Nuevo Usuario?', new_user_path %>
    </div>
<% end %>